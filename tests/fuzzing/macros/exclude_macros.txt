# Keep assertions enabled during fuzzing (NDEBUG would disable them).
# Note: PRINTF(...)=, USE_OS_IO_STACK, and HAVE_SHA512_WITH_BLOCK_ALT_METHOD
# are already excluded by the SDK's macros/exclude_macros.txt.
NDEBUG
