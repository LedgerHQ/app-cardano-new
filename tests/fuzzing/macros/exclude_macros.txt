# Disable printf macro for fuzzing builds
PRINTF
PRINTF(...)=
# Keep assertions enabled during fuzzing (NDEBUG would disable them)
NDEBUG
# Breaks pic mock function (see SDK fuzzing/macros/exclude_macros.txt)
USE_OS_IO_STACK
