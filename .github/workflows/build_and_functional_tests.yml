name: Build and run functional tests using ragger through reusable workflow

# This workflow will build the app and then run functional tests using the Ragger framework upon Speculos emulation.
# It calls a reusable workflow developed by Ledger's internal developer team to build the application and upload the
# resulting binaries.
# It then calls another reusable workflow to run the Ragger tests on the compiled application binary.
#
# The build part of this workflow is mandatory, this ensures that the app will be deployable in the Ledger App Store.
# While the test part of this workflow is optional, having functional testing on your application is mandatory and this workflow and
# tooling environment is meant to be easy to use and adapt after forking your application

on:
  workflow_dispatch:
    inputs:
      golden_run:
        type: choice
        required: true
        default: 'Raise an error (default)'
        description: CI behavior if the test snapshots are different than expected.
        options:
          - 'Raise an error (default)'
          - 'Open a PR'
  push:
    branches:
      - master
      - develop
  pull_request:

jobs:
  check_generated_unit_test_files:
    name: Verify generated unit-test files are up to date
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ledgerhq/ledger-app-builder/ledger-app-dev-tools:latest
    steps:
      - name: Clone
        uses: actions/checkout@v4

      - name: Install Python dependencies for generator
        run: |
          python3 -m pip install --upgrade pip
          pip install --extra-index-url https://test.pypi.org/simple/ -r tests/standalone/requirements.txt

      - name: Regenerate unit-test files
        run: |
          cd unit-tests
          python3 generators/generate_unit_tests_from_ragger.py

      - name: Verify no generated file drift
        run: |
          if [ -n "$(git status --porcelain -- unit-tests)" ]; then
            echo "Generated unit-test files are out of date. Run unit-tests/generators/generate_unit_tests_from_ragger.py and commit the changes."
            git status --short -- unit-tests
            exit 1
          fi

  run_unit_tests_gate:
    name: Run unit tests
    needs:
      - check_generated_unit_test_files
    uses: LedgerHQ/ledger-app-workflows/.github/workflows/reusable_unit_tests.yml@v1
    secrets: inherit
    with:
      test_directory: unit-tests

  build_application:
    name: Build application using the reusable workflow
    uses: LedgerHQ/ledger-app-workflows/.github/workflows/reusable_build.yml@v1
    with:
      upload_app_binaries_artifact: "compiled_app_binaries"

  ragger_tests:
    name: Run standalone ragger tests using the reusable workflow
    needs:
      - check_generated_unit_test_files
      - run_unit_tests_gate
      - build_application
    uses: LedgerHQ/ledger-app-workflows/.github/workflows/reusable_ragger_tests.yml@v1
    with:
      download_app_binaries_artifact: "compiled_app_binaries"
      regenerate_snapshots: ${{ github.event_name == 'workflow_dispatch' && inputs.golden_run == 'Open a PR' }}
      test_dir: "tests/standalone"
